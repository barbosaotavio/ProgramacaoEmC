#include <stdio.h>

int main(){

    float a = 5.5;
    float b = 2.2;
    float soma = a + b;
    float diferenca = a - b;
    float produto = a * b;
    float quociente = a / b;

    printf("A soma é: %.2f\n", soma);
    printf("A diferença é: %.2f\n", diferenca);
    printf("O produto é: %.2f\n", produto);
    printf("O quociente é: %.2f\n", quociente); // Divisão de ponto flutuante

}
